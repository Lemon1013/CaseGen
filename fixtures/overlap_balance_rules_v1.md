# 余额交易规则（版本一）

本文件用于验证同一业务规则的首次摄入。规则页面的稳定身份应由 `rule.order.balance` 表示，而不是由上传文件名生成。

## 4.1 余额校验

4.1 现货限价买单提交时，系统比较账户可用余额与订单所需金额。可用余额为 100 元、订单所需金额为 600 元时，应拒绝下单，返回余额不足。

## 4.2 冻结规则

4.2 订单通过余额校验后，系统冻结订单所需的报价资产；拒单不得改变可用余额或冻结余额。
