# 余额交易规则（版本二）

本文件与版本一有重叠内容，用于验证增量摄入应识别已有 `rule.order.balance` 页面，并进入 update/review 流程，不得按新文件再建一页。

## 4.1 余额校验

4.1 现货限价买单提交时，系统比较账户可用余额与订单所需金额。可用余额为 120 元、订单所需金额为 600 元时，仍应拒绝下单，返回余额不足。

## 4.2 冻结规则

4.2 订单通过余额校验后，系统冻结订单所需的报价资产；拒单不得改变可用余额或冻结余额。
